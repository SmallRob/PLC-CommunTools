<UserControl x:Class="Commun.Client.WPF.Views.ModbusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource MetroWindowBackgroundBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GroupBox Header="连接" Grid.Row="0" Style="{DynamicResource MetroGroupBox}">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="主机:" Style="{DynamicResource MetroLabel}" />
                <TextBox x:Name="TxtHost" Text="192.168.1.100" Width="150" Style="{DynamicResource MetroTextBox}" />
                <Label Content="端口:" Style="{DynamicResource MetroLabel}" Margin="10,0,0,0" />
                <TextBox x:Name="TxtPort" Text="502" Width="60" Style="{DynamicResource MetroTextBox}" />
                <Button x:Name="BtnConnect" Content="连接" Style="{DynamicResource MetroButton}" Margin="10,0,0,0" Click="BtnConnect_Click" />
            </StackPanel>
        </GroupBox>
        <GroupBox Header="读写" Grid.Row="1" Style="{DynamicResource MetroGroupBox}">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="地址:" Style="{DynamicResource MetroLabel}" />
                <TextBox x:Name="TxtRegister" Text="0" Width="60" Style="{DynamicResource MetroTextBox}" />
                <Label Content="值/长度:" Style="{DynamicResource MetroLabel}" Margin="10,0,0,0" />
                <TextBox x:Name="TxtValue" Text="1" Width="60" Style="{DynamicResource MetroTextBox}" />
                <Button x:Name="BtnRead" Content="读取" Style="{DynamicResource MetroButton}" Margin="10,0,0,0" Click="BtnRead_Click" />
                <Button x:Name="BtnWrite" Content="写入" Style="{DynamicResource MetroButton}" Margin="5,0,0,0" Click="BtnWrite_Click" />
            </StackPanel>
        </GroupBox>
        <TextBox x:Name="TxtLog" Grid.Row="2" IsReadOnly="True" VerticalScrollBarVisibility="Auto" Background="#FF1E1E1E" Foreground="#FF00FF00" FontFamily="Consolas" FontSize="12" />
    </Grid>
</UserControl>
